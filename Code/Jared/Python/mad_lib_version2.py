import random

adj = input('Give me six adjectives separated by commas: ')
adj_list = adj.split(',')
print(adj_list)

#noun = input('Give me six nouns separated by commas: ')
#noun_list = noun.split(',')

#verb = input('Give me six verbs separted by commas: ')
#verb_list = verb.split(',')

#print('So much ' + random.choice(verb_list) + ' upon a ' + random.choice(adj_list) + ' ' + random.choice(noun_list), + ' ' + random.choice(verb_list) + 'with ' + random.choice(noun_list) + ' besides the ' + random.choice(adj_list) + ' ' + random.choice(noun_list).)
